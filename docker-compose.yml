version: '3.8'

services:
  # --------------------------------------------------------------------------
  # ⚡ FrankenPHP Web Server (Laravel Application)
  # Port Container: 8080 -> Port Host: 8095 (Aman & Tidak Bentrok di 1Panel)
  # --------------------------------------------------------------------------
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cuan_money_app
    restart: always
    ports:
      - "127.0.0.1:8095:8080"
    environment:
      APP_ENV: production
      APP_DEBUG: "false"
      APP_URL: "https://cuan.humacode.my.id"
      DB_HOST: cuan_money_db
      DB_PORT: 3306
      DB_DATABASE: money_management_v2
      DB_USERNAME: money_user
      DB_PASSWORD: ${DB_PASSWORD:-SecretMoney2026!}
      SERVER_NAME: "cuan.humacode.my.id"
    volumes:
      - ./storage:/app/storage
      - ./bootstrap/cache:/app/bootstrap/cache
    depends_on:
      - db
    networks:
      - cuan_network

  # --------------------------------------------------------------------------
  # 🗄️ MySQL Database
  # Port Container: 3306 -> Port Host: 3309 (Aman dari 3306, 3307, 3308 di 1Panel)
  # --------------------------------------------------------------------------
  db:
    image: mysql:8.0
    container_name: cuan_money_db
    restart: always
    ports:
      - "127.0.0.1:3309:3306"
    environment:
      MYSQL_DATABASE: money_management_v2
      MYSQL_USER: money_user
      MYSQL_PASSWORD: ${DB_PASSWORD:-SecretMoney2026!}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-RootMoney2026!}
    volumes:
      - db_data:/var/lib/mysql
    networks:
      - cuan_network

networks:
  cuan_network:
    driver: bridge

volumes:
  db_data:
    driver: local
